type Query {
  getMessages: [Message!]
}

type Message {
  _id: ID!
  title: String!
  content: String!
  author: String!
}

type Mutation {
  createMessage(title: String!, content: String!, author:String!): Message
}
